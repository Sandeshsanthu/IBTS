# Code Generated by Sidekick is for learning and experimentation purposes only.

cd C:\Users\sandeshs\IBTS

# -- save the file first ----------------------------------------------------
# copy-paste your yml above into this file
Set-Content .github\workflows\pr-checks.yml -Value (Get-Clipboard)

# -- verify file saved correctly --------------------------------------------
Get-ChildItem .github\workflows\
Select-String "on:" .github\workflows\pr-checks.yml | Select-Object -First 3

# -- stage + commit + push --------------------------------------------------
git add .github\workflows\pr-checks.yml
git status
git commit -m "ci: fix pr-checks.yml encoding + trigger on push to main"
git push origin main

