# Code Generated by Sidekick is for learning and experimentation purposes only.
# filename: app/config.py

from pydantic_settings import BaseSettings


class Settings(BaseSettings):
    # Upstream
    PAYMENT_SWITCH_URL: str = "http://payment-switch:8083"
    UPSTREAM_TIMEOUT: float = 10.0

    # Redis (shared with payment-router)
    REDIS_HOST: str = "redis"
    REDIS_PORT: int = 6379

    # Auth
    JWT_SECRET: str = "ibts-dev-secret-change-in-prod"
    JWT_ALGORITHM: str = "HS256"
    API_KEY: str = "ibts-api-key-dev"          # X-API-Key header fallback

    # Rate limits (sliding window)
    RL_PER_IP_REQUESTS: int = 100
    RL_PER_VPA_REQUESTS: int = 10
    RL_WINDOW_SECONDS: int = 60

    # Service
    PORT: int = 8084
    ENV: str = "local"

    class Config:
        env_file = ".env"


settings = Settings()
