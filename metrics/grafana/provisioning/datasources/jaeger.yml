# Code Generated by Sidekick is for learning and experimentation purposes only.
# filename: metrics/grafana/provisioning/datasources/jaeger.yml

apiVersion: 1

datasources:
  - name:      Jaeger
    type:      jaeger
    uid:       jaeger              # ← matches exemplarTraceIdDestinations above
    access:    proxy
    url:       http://ibts-jaeger:16686
    isDefault: false               # ← never true, Prometheus owns the default
    editable:  false
    jsonData:
      tracesToLogsV2:
        datasourceUid: elasticsearch   # future: link traces → logs
